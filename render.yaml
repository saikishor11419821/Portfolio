services:
  - type: web
    runtime: static
    name: kishor-portfolio
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
